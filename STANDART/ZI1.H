#ifndef _zi_h
#define _zi_h

struct ZI
 {
   word in ;               // Вход ЗИ / Вход 2-го интегратора ЗИ .
   word out_lin  , out1 ;    // Выход линейного ЗИ / выход 2-го интегратора / выход параболического ЗИ .
   word out  ;            // Выход линейного ЗИ / выход 2-го интегратора / выход параболического ЗИ .
   word drob_lin ;        // Дробная часть линейного выхода ЗИ
//   word drob1 ;           // Дробная часть линейного выхода ЗИ / Дробная часть выхода 2-го интегратора.
   word *temp_p_razg ;    // Адрес уставки тепма разгона в "+" , размерность которой: "дискреты в секунду" .
   word *temp_p_torm ;    // Адрес уставки тепма торможения в "+", размерность которой: "дискреты в секунду" .
   word *temp_m_razg ;    // Адрес уставки тепма разгона в "-", размерность которой: "дискреты в секунду" .
   word *temp_m_torm ;    // Адрес уставки тепма торможения в "-", размерность которой: "дискреты в секунду".
   word *temp_parabola ;      // Адрес уставки постоянной времени параболической части ЗИ, размерность: "милисекунды" .
   word *temp_parabola_stop ; // Адрес уставки постоянной времени параболической части ЗИ, размерность: "милисекунды" .
   word *temp_parabola_delta; // Адрес уставки максимально "желаемого" отставания выхода параболы от ее входа, размерность: "дискреты" .
   word  temp_parabola_old ;      // Адрес уставки постоянной времени параболической части ЗИ, размерность: "милисекунды" .
   word  ds_old ;
   double s_out ; // Дробная часть выхода S-ЗИ .
   word  time  ;           // Время последней отработки ЗИ.
   word  time_szi;//, proizv ;
 } ;

void Zad_Intens ( struct ZI *zip ) ;

void Set_ZI ( struct ZI *zip , word ) ;


#endif
